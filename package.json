{
  "name": "@adefw/stringkit",
  "version": "0.1.0",
  "author": "adefw",
  "license": "MIT",
  "description": "string kit for browser and nodejs.",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "directories": {
    "test": "test"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/preset-env": "^7.9.5",
    "@babel/preset-typescript": "^7.9.0",
    "@types/jest": "^25.2.1",
    "jest": "^25.3.0",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "build": "tsc -p src --outDir dist",
    "test": "jest",
    "coverage": "jest --coverage",
    "prepack": "npm run test && npm run build"
  },
  "keywords": [
    "stringkit",
    "fullwidth",
    "halfwidth"
  ]
}
